# 30. Write a Python program that will accept the base and height of a triangle and compute its 

def areaOfRect(length,breadth):
 length=int(input('Enter the length'))
 breadth=int(input('Enter the breadth'))
 a = length * breadth
 return a
 